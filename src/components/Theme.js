import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#8B4513', 
    },
    background: {
      default: '#F5F5DC', 
    },
  },
});

export default theme;
